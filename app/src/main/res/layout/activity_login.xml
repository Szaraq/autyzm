<?xml version="1.0" encoding="utf-8"?>
<!-- TODO Zobaczyć czy działa scrollowanie przy klawiaturze software'owej (to samo w userDetails), jeżeli nie to http://stackoverflow.com/questions/2417221/adding-view-to-bottom-of-layout-inside-a-scrollview -->

<android.support.percent.PercentRelativeLayout
    android:id="@+id/containerLayout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    tools:context="pl.osik.autyzm.login.LoginActivity">

    <ImageView
        android:id="@+id/backgroundImage"
        android:scaleType="centerCrop"
        android:adjustViewBounds="true"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <TextView
        android:id="@+id/logo"
        app:layout_heightPercent="30%"
        android:textSize="60sp"
        android:gravity="center"
        android:textColor="@color/colorAppBackground"
        android:text="@string/app_name"
        android:layout_width="match_parent"
        android:layout_height="0dp" />

    <FrameLayout
        android:id="@+id/userPhotoContainer"
        android:layout_below="@id/logo"
        app:layout_heightPercent="20%"
        android:layout_width="match_parent"
        android:layout_height="0dp">

        <de.hdodenhof.circleimageview.CircleImageView
            android:id="@+id/userPhoto"
            android:layout_gravity="center"
            app:civ_border_width="0dp"
            android:layout_width="96dp"
            android:layout_height="96dp" />

    </FrameLayout>

    <FrameLayout
        android:layout_alignParentBottom="true"
        android:id="@+id/buttonContainer"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <Button
            android:id="@+id/button"
            style="@style/buttonStyle.bottomButton"
            android:text="@string/login_zaloguj" />

    </FrameLayout>

    <LinearLayout
        android:layout_above="@id/buttonContainer"
        android:layout_below="@id/userPhotoContainer"
        android:id="@+id/linearLayout2"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="20dp"
        android:orientation="vertical"
        android:padding="20dp">

        <android.support.design.widget.TextInputLayout
            android:id="@+id/loginLayout"
            android:theme="@style/TextDesignLabel"
            android:layout_width="match_parent"
            android:layout_height="wrap_content">

            <android.support.design.widget.TextInputEditText
                android:id="@+id/login"
                android:layout_marginBottom="40dp"
                style="@style/EditTextMaterial.EditTextLogin"
                android:hint="@string/login_login" />

        </android.support.design.widget.TextInputLayout>

        <android.support.design.widget.TextInputLayout
            android:id="@+id/passLayout"
            android:theme="@style/TextDesignLabel"
            android:layout_width="match_parent"
            android:layout_height="wrap_content">

            <android.support.design.widget.TextInputEditText
                android:id="@+id/password"
                style="@style/EditTextMaterial.EditTextLogin"
                android:layout_marginBottom="10dp"
                android:hint="@string/login_haslo"
                android:inputType="textPassword" />

        <!-- android:layout_height="wrap_content"
            android:layout_width="match_parent"
            android:background="@color/colorAppBackground"
            android:textColor="@color/colorPrimary"
            -->

        </android.support.design.widget.TextInputLayout>

        <TextView
            android:id="@+id/error"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_below="@id/linearLayout2"
            android:layout_marginTop="5dp"
            android:text="@string/login_error"
            android:textAlignment="center"
            android:textColor="@color/colorError"
            android:visibility="invisible" />

        <TextView
            android:id="@+id/noweKonto"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_below="@id/error"
            android:layout_marginTop="30dp"
            android:gravity="center"
            android:text="@string/login_nowe_konto"
            android:textColor="@color/colorAppBackground" />

    </LinearLayout>

</android.support.percent.PercentRelativeLayout>